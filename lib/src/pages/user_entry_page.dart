import 'package:fluent_ui/fluent_ui.dart';

class UserEntryPage extends StatelessWidget {
  const UserEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ScaffoldPage(content: Center(child: Text("用户录入")),);
  }
}