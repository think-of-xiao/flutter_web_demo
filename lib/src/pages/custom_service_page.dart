import 'package:fluent_ui/fluent_ui.dart';

class CustomServicePage extends StatelessWidget {
  const CustomServicePage({super.key});

  @override
  Widget build(BuildContext context) {
    return ScaffoldPage(
      content: Center(child: Text("客服系统")),
    );
  }
}
