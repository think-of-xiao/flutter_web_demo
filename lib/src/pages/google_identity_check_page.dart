import 'package:fluent_ui/fluent_ui.dart';

class GoogleIdentityCheckPage extends StatelessWidget {
  const GoogleIdentityCheckPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ScaffoldPage(content: Center(child: Text("google身份验证")),);
  }
}